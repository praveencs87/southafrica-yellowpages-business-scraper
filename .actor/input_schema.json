{
    "title": "South Africa Fyple Business Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "startUrls": {
            "title": "Start URLs",
            "type": "array",
            "description": "List of search URLs to scrape.",
            "editor": "requestListSources",
            "prefill": [
                { "url": "https://www.fyple.co.za/search/cape-town/plumber/" }
            ]
        },
        "maxLeads": {
            "title": "Maximum Leads",
            "type": "integer",
            "description": "Maximum number of South African business leads to extract.",
            "default": 100,
            "minimum": 1
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration (Optional)",
            "type": "object",
            "description": "Optional proxy settings. Fyple.co.za does not require proxies for standard usage.",
            "editor": "proxy"
        }
    },
    "required": ["startUrls"]
}
