{
    "title": "South Africa Fyple Business Scraper Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "keyword": {
            "title": "Search Query (What)",
            "type": "string",
            "description": "The specific business category or name (e.g., 'plumber', 'mining', 'attorney').",
            "editor": "textfield",
            "prefill": "plumber"
        },
        "location": {
            "title": "Location (Where)",
            "type": "string",
            "description": "City in South Africa (e.g., 'Cape Town', 'Johannesburg', 'Durban', 'Pretoria', 'Sandton').",
            "editor": "textfield",
            "prefill": "Cape Town"
        },
        "maxLeads": {
            "title": "Maximum Leads",
            "type": "integer",
            "description": "Maximum number of South African business leads to extract.",
            "default": 100,
            "minimum": 1
        },
        "proxyConfiguration": {
            "title": "Proxy Configuration (Optional)",
            "type": "object",
            "description": "Optional proxy settings. Fyple.co.za does not require proxies for standard usage.",
            "editor": "proxy"
        }
    },
    "required": ["keyword", "location"]
}
